"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.notifications_list = void 0;
const auth_1 = require("../../utility/auth");
const handleErrors_1 = require("../../utility/handleErrors");
const request_1 = require("../../utility/request");
const notifications_list = (params, addons) => __awaiter(void 0, void 0, void 0, function* () {
    if (auth_1.credentials.type != 'lazer') {
        return (0, handleErrors_1.handleErrors)(new Error(`Login via lazer to use this endpoint`));
    }
    ;
    const data = yield (0, request_1.request)(`https://osu.ppy.sh/api/v2/notifications`, {
        method: 'GET',
        params: {
            max_id: params === null || params === void 0 ? void 0 : params.max_id
        },
        addons,
    });
    if (data.error)
        return (0, handleErrors_1.handleErrors)(new Error(data.error));
    if ((params === null || params === void 0 ? void 0 : params.unread_only) == true && !('error' in data))
        data.notifications = data.notifications.filter((r) => r.is_read == false);
    return data;
});
exports.notifications_list = notifications_list;
