"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.chat_updates = void 0;
const auth_1 = require("../../utility/auth");
const handleErrors_1 = require("../../utility/handleErrors");
const request_1 = require("../../utility/request");
const chat_updates = (params, addons) => __awaiter(void 0, void 0, void 0, function* () {
    if (auth_1.credentials.type != 'lazer') {
        return (0, handleErrors_1.handleErrors)(new Error(`Login via lazer to use this endpoint`));
    }
    ;
    if ((params === null || params === void 0 ? void 0 : params.after_id) == null) {
        return (0, handleErrors_1.handleErrors)(new Error(`Specify after_id`));
    }
    ;
    const obj = {};
    if (params === null || params === void 0 ? void 0 : params.history_since)
        obj['history_since'] = params.history_since;
    if (params === null || params === void 0 ? void 0 : params.includes)
        obj['includes[]'] = params.includes;
    if (params === null || params === void 0 ? void 0 : params.after_id)
        obj['since'] = params.after_id;
    const data = yield (0, request_1.request)(`https://osu.ppy.sh/api/v2/chat/updates`, {
        method: 'GET',
        params: obj,
        addons,
    });
    if (data.error)
        return (0, handleErrors_1.handleErrors)(new Error(data.error));
    return data;
});
exports.chat_updates = chat_updates;
