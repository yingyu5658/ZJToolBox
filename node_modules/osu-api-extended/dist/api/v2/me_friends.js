"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.me_friends = void 0;
const auth_1 = require("../../utility/auth");
const request_1 = require("../../utility/request");
const handleErrors_1 = require("../../utility/handleErrors");
const me_friends = (addons) => __awaiter(void 0, void 0, void 0, function* () {
    if (auth_1.credentials.type != 'lazer' && auth_1.credentials.type != 'cli') {
        return (0, handleErrors_1.handleErrors)(new Error('Login via lazer or cli to use this endpoint'));
    }
    ;
    if (auth_1.credentials.type == 'cli' && !auth_1.credentials.scopes.includes('friends.read')) {
        return (0, handleErrors_1.handleErrors)(new Error(`Requires "friends.read" scope`));
    }
    ;
    const data = yield (0, request_1.request)(`https://osu.ppy.sh/api/v2/friends`, {
        method: 'GET',
        addons,
    });
    if (data.error)
        return (0, handleErrors_1.handleErrors)(new Error(data.error));
    return data;
});
exports.me_friends = me_friends;
