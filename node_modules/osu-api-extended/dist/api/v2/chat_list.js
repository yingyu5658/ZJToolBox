"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.chat_list = void 0;
const handleErrors_1 = require("../../utility/handleErrors");
const request_1 = require("../../utility/request");
const chat_list = (params, addons) => __awaiter(void 0, void 0, void 0, function* () {
    let data = yield (0, request_1.request)(`https://osu.ppy.sh/api/v2/chat/presence/`, {
        method: 'GET',
        addons
    });
    if (data.error)
        return (0, handleErrors_1.handleErrors)(new Error(data.error));
    if ((params === null || params === void 0 ? void 0 : params.sort) == 'date_asc')
        data.sort((a, b) => a.last_message_id - b.last_message_id);
    else
        data.sort((a, b) => b.last_message_id - a.last_message_id);
    if ((params === null || params === void 0 ? void 0 : params.unread) == true)
        data = data.filter((r) => r.last_read_id != r.last_message_id);
    return data;
});
exports.chat_list = chat_list;
