import { BeatmapsEvents } from "../../types/v2/beatmaps_events_list";
import { IDefaultParams, IError, beatmap_events_types } from "../../types";
export declare const beatmaps_events_list: (obj: {
    user: string | number;
    types: (beatmap_events_types)[];
    min_date: string;
    max_date: string;
}, addons?: IDefaultParams) => Promise<BeatmapsEvents & IError>;
